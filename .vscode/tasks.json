{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "C compile",
            "type": "shell",
            "command": "gcc ${relativeFile}  -o ${fileBasenameNoExtension} -ansi -Wall -pedantic",
            "group": "build",
            "problemMatcher": []
        }
    ]
}